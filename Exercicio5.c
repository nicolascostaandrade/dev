#include <stdio.h>

int main () {
    int nota;

    printf("Digite o número ");
    scanf("%d", "média");

    printf("O aluno está aprovado? (s/n): ");
    scanf(" %c", &resp);
    if (resp == 's || resp == 'S') {
        contador++;
        
    If else ||("O aluno não está aprovado? (s/m): ");
    printf(" %c", &resp);
    if ()